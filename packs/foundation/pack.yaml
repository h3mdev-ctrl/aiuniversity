# foundation -- set up Claude properly the first time (the umbrella pack).
#
# The flagship: the layers that make Claude durable and useful over months, not
# just working once. Layer 4 (capabilities) declares a MODULE SLOT and threads
# the real gbrain-windows pack through it -- proving composition against a real
# module, which is the whole point of this pack existing.
#
# v1 scope: this is a working slice, not all six layers at full depth. It proves
# the umbrella + a real module run end to end. Identity/memory/guardrails layers
# (and their behavioural cold-recall probes) are the next authoring pass.
name: foundation
version: 0.1.0
steps:
  - id: layer-1-memory
    instruction: >
      Memory that compounds: a memory/ folder with an always-loaded resolver index,
      wired into your constitution and proven to actually recall. This runs the
      memory module end to end -- set it up, audit it, and check it's live.
    module: memory

  - id: layer-0-identity
    instruction: >
      Who you are + how Claude should talk to you (layer 0). Runs the identity
      interview, writes your context files, and files a user_profile into memory.
      Sequenced AFTER memory on purpose -- it files a profile INTO the memory the
      previous step created.
    module: identity

  - id: layer-2-constitution
    instruction: >
      Your constitution: a global CLAUDE.md that tells Claude your environment and hard
      rules, AND -- the sections newcomers always miss -- an OPERATING PRINCIPLE block
      (verify + plan before executing; shoulders-of-giants; recon before build; present
      decisions, don't pre-make them; don't tell the user when to stop), SKILL ROUTING
      (invoke a matching skill FIRST, with a trigger table), and a gbrain hub pointer.
      This step seeds them so Claude follows a known path instead of improvising. Edit the
      trigger table to your own skills after; add your OS/shell + hard rules alongside.
    check:
      type: command_succeeds
      cmd: python packs/foundation/files/setup_constitution.py --check
    fix: python packs/foundation/files/setup_constitution.py --install
    on_fail: >
      Your CLAUDE.md is missing the constitution core (operating principle + skill routing
      + gbrain hub). Seed it (idempotent; coexists with your existing rules):
      python packs/foundation/files/setup_constitution.py --install
      Then add your environment (OS, shell) + hard rules, and edit the skill-routing
      table to point at YOUR skills.

  - id: layer-3-guardrails
    instruction: >
      Automatic backstops that catch mistakes by CODE, not by hoping. Installs a
      PreToolUse hook that blocks reading credential files (.env, private keys)
      and proves it fires by piping a forbidden probe through it.
    module: guardrails

  - id: layer-4-capabilities-note
    instruction: >
      Capabilities (gbrain): the foundation is complete without gbrain, but most
      setups include it -- it gives Claude a persistent, searchable knowledge brain
      that compounds across sessions and is the memory backend for several other
      packs. If you have gbrain installed (or plan to), run the gbrain pack now as
      a follow-on:

        python -m runner.cli remediate packs/gbrain-windows

      Skip this if you are not using gbrain. It is not required for the foundation
      to function.
    check:
      type: command_succeeds
      cmd: python -c "import shutil; exit(0 if shutil.which('gbrain') else 0)"
    on_fail: >
      gbrain is not on your PATH. That is fine -- foundation is complete without it.
      When you are ready, run: python -m runner.cli remediate packs/gbrain-windows

  - id: layer-5-durability
    instruction: >
      Durability: the projects folder where Claude's per-project memory compounds
      over time, so it improves instead of restarting from scratch each session.
    check:
      type: command_succeeds
      cmd: |
        python -c "import pathlib,sys; sys.exit(0 if (pathlib.Path.home()/'.claude'/'projects').exists() else 1)"
    on_fail: >
      No ~/.claude/projects folder yet. It appears once you've run Claude Code in
      a project; run a session in a project first, then re-check.
