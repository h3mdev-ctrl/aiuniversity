# hooks -- opt-in behavioural guards that catch mistakes by CODE, not by hoping.
#
# guardrails (foundation layer-3) installs the mandatory baseline: credential_guard
# blocks Claude from reading .env / private keys. This pack surfaces the opt-in
# hooks that ship alongside it -- things you might want once you've seen the
# baseline working. Each step is self-contained: install + prove it fires.
#
# These are NOT required for Claude to function. They're levers you pull when a
# soft principle in your CLAUDE.md constitution keeps getting ignored and you want
# a hard backstop that fires at the moment of the tool call.
#
# Run guardrails first (foundation layer-3 does this).
# Then install whichever hooks fit your workflow.
#
# BEFORE YOU WRITE OR EDIT A GUARD: read files/GUARD_DESIGN.md. Eight rules, each
# paid for with a real defect on a live machine (~30 hooks, 46,857 tool calls,
# 441 sessions) inside a single day. The two that cost most:
#
#   * A broken guard and a quiet guard look IDENTICAL. PreToolUse treats an
#     unexpected exit as a NON-BLOCKING error, so a crashed guard PERMITS while
#     everything still looks fine. One crashed on every call for a day.
#   * Narrow a guard, never blind it -- and pin the narrowing with a POSITIVE
#     control. A fix for a cosmetic false positive taught a guard to ignore
#     heredoc bodies, which opened a bypass letting `python - <<PY` rewrite any
#     hook. The tests passed: they pinned the annoyance and never pinned the
#     attack.
#
# The `hook-liveness` step below is rule 1 made runnable.
name: hooks
version: 0.1.0
steps:
  - id: recon-before-build
    instruction: >
      Recon-before-build guard: fires when Claude's first Write in a session would
      create a NEW source file (.py, .ts, .js, .go, .rs, .rb ...) in a repo that
      already has code. It exits non-zero with a nudge to read the neighbouring
      code before building a parallel version of it -- the most common way Claude
      re-derives logic that already exists 40 lines away.

      The soft version ships in the foundation constitution ("Recon before build").
      Install this hard hook when the soft principle keeps getting skipped and you
      want it to fire at the moment of the Write, not just as a guideline.

      Installs: ~/.claude/hooks/recon_before_build_guard.py  +
                PreToolUse(Write) entry in ~/.claude/settings.json.
    check:
      type: command_succeeds
      cmd: python packs/guardrails/files/setup_recon_build_guard.py --check-hook-file
    fix: python packs/guardrails/files/setup_recon_build_guard.py
    on_fail: >
      Hook file missing. Install it (idempotent):
        python packs/guardrails/files/setup_recon_build_guard.py
      Then verify it fires:
        python packs/guardrails/files/setup_recon_build_guard.py --test-blocking

  - id: recon-before-build-fires
    instruction: >
      Prove the recon guard actually fires -- pipe a simulated first-Write-of-a-new-
      file through the installed hook and confirm it exits non-zero with a BLOCKED
      message. Also confirm that overwriting an existing file still passes (so it's
      a real gate, not a wall).
    check:
      type: command_succeeds
      cmd: python packs/guardrails/files/setup_recon_build_guard.py --test-blocking
    on_fail: >
      The recon guard didn't block a known-forbidden action. Reinstall:
        python packs/guardrails/files/setup_recon_build_guard.py

  - id: hook-liveness
    instruction: >
      Install the hook doctor and confirm every hook you have registered is
      actually ALIVE. This is the step people skip, and it is the one that pays
      off most over time: a hook that crashes and a hook with nothing to say look
      IDENTICAL from the outside -- both are silent. For PreToolUse an unexpected
      exit is treated as a NON-BLOCKING error, so the tool call proceeds
      UNGUARDED while everything still looks normal. The doctor feeds each
      registered hook a deliberately benign payload for its event and fails if it
      crashes or returns anything other than allow/block.
    check:
      type: contains
      cmd: python packs/hooks/files/hook_doctor.py
      expect: "VERDICT: HEALTHY"
    on_fail: >
      At least one registered hook is BROKEN -- the report names the hook and the
      error. Usual causes: (a) the file moved or was renamed but settings.json
      still points at the old path; (b) a half-applied edit left it uncompilable;
      (c) an edit changed a data structure the dispatch loop reads without
      updating the loop -- e.g. adding a callable matcher to a list the loop still
      calls `.search()` on, which raises AttributeError on EVERY call and silently
      disables the entire hook. Fix the named hook, then re-run.
      Run this after ANY hook edit and at every wrap-up. On the machine this came
      from, two hooks were dead at the same time for a full day and both were
      found by accident, not by anything reporting them.

  - id: guard-correctness
    instruction: >
      Install the guard regression harness and confirm every guard you have
      still CATCHES what it was built for. The doctor above answers "is this
      hook alive?"; this answers "does it still fire on the thing it exists
      for, and still ignore the near-miss?" A hook can be perfectly alive with
      a matcher that no longer matches ANYTHING -- which is what a dark
      advisory list looks like from the outside, and is exactly what happened
      on the machine these rules came from when predicates were added to a list
      the dispatch loop still called `.search()` on.

      This is rule 6 of files/GUARD_DESIGN.md made runnable, the way
      `hook-liveness` is rule 1. Run BOTH after any hook edit and at wrap-up.

      Read the verdict line, not just the colour: a guard you have not
      installed produces SKIPPED cases, never passing ones, and a hook you HAVE
      registered with no cases is reported as UNCOVERED. Neither is a failure;
      both are things you cannot claim you checked. Add your own cases in
      $CLAUDE_HOME/guard_cases.json -- one per footgun you have actually hit,
      each with the near-miss that must stay silent next to it.
    check:
      type: contains
      cmd: python packs/hooks/files/guard_regression.py
      expect: "VERDICT: HEALTHY"
    on_fail: >
      A FAIL means a footgun you have already hit is no longer caught. Run
      `python packs/hooks/files/hook_doctor.py` FIRST -- a DEAD hook fails every
      one of its cases, and that is a different repair from a matcher that
      stopped matching. If the doctor is HEALTHY, the guard is alive and its
      matcher has drifted: read the failing case, decide whether the rule or the
      case is wrong, and fix that one. Never delete a case to get to green --
      that is how a guard goes dark without anyone noticing.

  - id: windows-composition-guard
    instruction: >
      Windows only. The `windows-shell` pack DOCUMENTS the shell-composition
      traps (heredocs mangling Python string literals, PowerShell here-strings
      pasted into bash, `$_` pre-expanded by bash before PowerShell sees it,
      text=True with no encoding=). Documentation cannot reach any of them: they
      are mistakes made WHILE COMPOSING A TOOL CALL, mid-turn, and a rule read at
      the start of a turn has no attachment point there. This installs the
      enforcement half.

      Blocks the three compositions that are already broken before they run --
      each has a correct alternative that is always available, so there is no
      bypass -- and advises on the code patterns that bite later.

      Installs: <CLAUDE_HOME>/hooks/windows_quirk_guard.py  +
                PreToolUse(Bash|PowerShell|Write|Edit) in settings.json.
    check:
      type: command_succeeds
      cmd: python packs/guardrails/files/setup_windows_quirk_guard.py --check-hook-file
    fix: python packs/guardrails/files/setup_windows_quirk_guard.py
    on_fail: >
      Hook file missing. Install it (idempotent):
        python packs/guardrails/files/setup_windows_quirk_guard.py
      Then prove it fires AND stays quiet:
        python packs/guardrails/files/setup_windows_quirk_guard.py --test-blocking
      Not on Windows? Skip this step -- every rule in it is a Windows or
      Windows-shell fact, and installing it elsewhere buys you nothing.

  - id: windows-composition-guard-fires
    instruction: >
      Prove it, both directions. The positive cases are worthless on their own:
      a guard that blocked every command would pass all three. So the same run
      asserts the near-misses stay SILENT -- a valid heredoc, a prose heredoc
      feeding `git commit -F -`, an ordinary command, and a .md file that merely
      DESCRIBES the traps. That last one is not hypothetical: an earlier version
      scanned prose the way it scanned code and fired three times on a docs page
      explaining these very rules. A guard that cries wolf on its own
      explanation gets ignored.
    check:
      type: command_succeeds
      cmd: python packs/guardrails/files/setup_windows_quirk_guard.py --test-blocking
    on_fail: >
      The guard misbehaved on a known case -- the output names which one and in
      which direction. A missed BLOCK means the matcher drifted; a fired ALLOW
      means it is too broad and will be ignored within a week (GUARD_DESIGN
      rule 3). Reinstall, then re-run:
        python packs/guardrails/files/setup_windows_quirk_guard.py

  - id: session-end
    instruction: >
      Session-end guard: a Stop hook that fires whenever Claude's response contains
      phrases like "good night", "get some rest", or "want to wrap up?" -- and exits
      non-zero to flag it. Stops Claude from pre-empting the user's decision about
      when to stop.

      The soft version ships in the constitution ("Don't tell the user when to stop").
      Install this when the soft principle keeps getting ignored and you want the
      Stop hook to catch it.

      Installs: ~/.claude/hooks/session_end_guard.py  +
                Stop entry in ~/.claude/settings.json.
    check:
      type: command_succeeds
      cmd: python packs/guardrails/files/setup_session_guard.py --check-hook-file
    fix: python packs/guardrails/files/setup_session_guard.py
    on_fail: >
      Hook file missing. Install it (idempotent):
        python packs/guardrails/files/setup_session_guard.py

  - id: session-end-fires
    instruction: >
      Prove the session-end guard actually fires -- pipe a response containing a
      session-end phrase through the hook and confirm it exits non-zero. Also
      confirm a normal response still passes.
    check:
      type: command_succeeds
      cmd: python packs/guardrails/files/setup_session_guard.py --test-blocking
    on_fail: >
      The session-end guard didn't block a known phrase. Reinstall:
        python packs/guardrails/files/setup_session_guard.py
